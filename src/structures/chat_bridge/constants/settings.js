/**
 * mc client version
 */
export const MC_CLIENT_VERSION = '1.16.5';

export const defaultSettings = Object.freeze({
	locale: 'en_US',
	viewDistance: 6, // tiny
	chatFlags: 0, // enabled
	chatColors: true,
	skinParts: 0,
	mainHand: 1,
});
