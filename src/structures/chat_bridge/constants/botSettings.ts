/**
 * mc client version
 */
export const MC_CLIENT_VERSION = '1.18';

export const DEFAULT_SETTINGS = {
	locale: 'en_US',
	viewDistance: 6, // tiny
	chatFlags: 0, // enabled
	chatColors: true,
	skinParts: 0,
	mainHand: 1,
} as const;
