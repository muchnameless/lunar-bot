'use strict';


module.exports = {
	aliases: [ 'comp' ],
	description: 'WIP',
	guildOnly: false,
	args: false,
	// usage: '',
	cooldown: 1,
	execute: async (message, args, flags) => {
		message.reply('WIP');
	},
};
